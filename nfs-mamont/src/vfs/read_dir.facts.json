{
  "module_path": "nfs_mamont::vfs::read_dir",
  "file_path": "/home/yarovoy/Documents/yadro/projects/oss/github/nfs-mamont/nfs-mamont/src/vfs/read_dir.rs",
  "public_structs": [
    {
      "name": "Success",
      "fields": "pub dir_attr: Option<file::Attr>, /// The cookie verifier.\n    pub cookie_verifier: CookieVerifier, /// Zero or more directory [`Entry`] entries. Represent linked list of [`Entry`]\n    pub entries: Vec<Entry>, /// `true` if the end of the directory has been reached for this request.\n    pub eof: bool",
      "methods": []
    },
    {
      "name": "Entry",
      "fields": "/// Since UNIX clients give a special meaning to the fileid\n    /// value zero, UNIX clients should be careful to map zero\n    /// fileid values to some other value and servers should try\n    /// to avoid sending a zero fileid.\n    pub file_id: u64, pub file_name: file::Name, pub cookie: Cookie",
      "methods": []
    },
    {
      "name": "Cookie",
      "fields": "",
      "methods": [
        "pub new (val: u64) -> Self",
        "pub raw (self) -> u64",
        "pub is_zero (self) -> bool"
      ]
    },
    {
      "name": "CookieVerifier",
      "fields": "",
      "methods": [
        "pub new (val: [u8; NFS3_COOKIEVERFSIZE]) -> Self",
        "pub raw (self) -> [u8; NFS3_COOKIEVERFSIZE]",
        "pub is_zero (self) -> bool"
      ]
    },
    {
      "name": "Fail",
      "fields": "/// Error on failure.\n    pub error: vfs::Error, /// The attributes of the directory, `dir`.\n    pub dir_attr: Option<file::Attr>",
      "methods": []
    },
    {
      "name": "Args",
      "fields": "/// The file handle for the directory to be read.\n    pub dir: file::Handle, /// This should be set to 0 in the first request to read the directory.\n    /// On subsequent requests, it should be a cookie as returned by the server.\n    pub cookie: Cookie, /// This should be set to 0 in the first request to read the directory.\n    /// On subsequent requests, it should be a cookie_verifier as returned by the server. The\n    /// cookie_verifier must match that returned by the [`ReadDir::read_dir`] in which the cookie\n    /// was acquired.\n    pub cookie_verifier: CookieVerifier, /// The maximum size of the [`Success`] structure, in bytes. The size must include\n    /// all XDR overhead. The server is free to return less than count bytes of data.\n    pub count: u32",
      "methods": []
    }
  ],
  "public_enums": [],
  "public_traits": [
    "#[trait_variant::make(Send)]\npub trait ReadDir {\n    /// Retrieves a variable number of entries, in sequence, from a directory.\n    ///\n    /// If the server detects that the cookie is no longer valid, the server will reject the\n    /// [`ReadDir::read_dir`] request with the status, [`vfs::Error::BadCookie`].\n    ///\n    /// The server may return fewer than [`Args::count`] bytes of XDR-encoded entries.\n    /// The [`Args::count`] specified by the client in the request should be greater than or equal to\n    /// the server's preferred [`ReadDir`] transfer size from\n    /// [`super::fs_info::Success::read_dir_pref`].\n    async fn read_dir(&self, args: Args) -> Result<Success, Fail>;\n}"
  ],
  "public_functions": []
}